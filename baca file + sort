#include <iostream>
#include <fstream>
#include <algorithm>
using namespace std;

int main (){
    int data[100], n=0;
    
    ifstream file("data.txt");
    while (file>> data[n]){
        n++;
    }
    file.close();
    sort(data, data+n);
    for (int i=0; i<n; i++){
        cout << data[i] << endl;
    }
}
